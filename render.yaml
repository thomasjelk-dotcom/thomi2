services:
  - type: web
    name: klassenraumchat
    runtime: python  # Wichtig: Explizit Python angeben!
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: FLASK_ENV
        value: production
    autoDeploy: true
